name: PR Validation
run-name: Validation for ${{ github.event.pull_request.title }}
on:
  pull_request:
    branches: [master]
jobs:
  validation:
    name: PR validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Dart SDK
        uses: dart-lang/setup-dart@v1
        with:
          sdk: "stable"

      - name: Validate example package
        working-directory: ./example
        run: |
          dart pub get
          dart run build_runner build
          dart analyze --fatal-warnings

      - name: Validate embed_annotation package
        working-directory: ./embed_annotation
        run: |
          dart pub get
          dart analyze --fatal-warnings

      - name: Validate embed package
        working-directory: ./embed
        run: |
          dart pub get
          dart run build_runner build
          dart test
          dart analyze --fatal-warnings

      - name: Check generated code is up to date
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "::error::Generated code is out of date. Run 'dart run build_runner build' and commit the changes."
            git status --porcelain
            git --no-pager diff
            exit 1
          fi
